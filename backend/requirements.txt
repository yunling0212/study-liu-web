# study-liu Web 版后端依赖
# 安装：pip install -r requirements.txt

# ---------- Web 框架 ----------
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9   # 文件上传（知识库导入 / 截图 OCR）必需

# ---------- 核心运行依赖（与桌面版一致） ----------
requests>=2.28.0
python-dotenv>=1.0.0

# ---------- API Key 加密（AES-GCM + PBKDF2） ----------
cryptography>=42.0.0

# ---------- 文档导入：图片 / PDF 解析 ----------
Pillow>=10.0.0
pypdf>=4.0.0
pdfplumber>=0.11.0

# ---------- 部署（Docker 内跑 gunicorn+uvicorn worker 更稳） ----------
gunicorn>=21.0.0

# ============================================================
# 可选依赖（服务器 2G 内存，默认不装）
# ============================================================
# OCR：easyocr 会连带 torch（约 2GB），2C2G 服务器装不动，
# Web 版"截图讲解"自动走"手动粘贴文字"兜底（已内置）。
# easyocr>=1.7.0
